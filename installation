# Debian

baggy@plasma:~$ sudo apt-get install cvs
baggy@plasma:~$ sudo apt-get install postfix
baggy@plasma:~$ sudo apt-get install rancid

# You will be presented with an informational text which says it is an early release of the package. It is however functional.
# The install will create a rancid user without a password. You will have to create a paasword for this user

baggy@plasma:~$ sudo passwd rancid
